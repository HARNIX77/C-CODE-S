#include <stdio.h>
int main()
{
    int integer;
    long longType;
    
    printf("size of int is %d bytes\n", sizeof (integer));
    printf("size of long is %d bytes\n", sizeof (longType));
    return 0;
    
}